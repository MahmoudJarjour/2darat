import { Container } from "@mui/material";
import React from "react";
import Layout from "../Components/Intro";

const Home = () => {
  return (
    <Container maxWidth="lg">
      <Layout />
    </Container>
  );
};

export default Home;
